#!/usr/bin/env ruby   # shebang line: tells interpreter we are running ruby

require_relative "../environment"

puts "Hi!"

cli_instance = CLI.new
cli_instance.start

# could also type this instead of above: CLI.new.start